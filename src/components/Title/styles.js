import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 2,
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    marginTop: 40,
    fontWeight: '800',
    fontSize: 30
  },
  subtitle: {
    fontWeight: '800',
    fontSize: 25
  }
});
